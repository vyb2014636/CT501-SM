import { styled } from '@mui/system'
import Button from '@mui/material/Button'

const ButtonFlexStar = styled(Button)({
  display: 'flex',
  justifyContent: 'flex-start',
  alignItems: 'center'
})

export default ButtonFlexStar
