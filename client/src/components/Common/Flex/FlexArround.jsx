import { styled } from '@mui/system'
import Box from '@mui/material/Box'

const FlexArrow = styled(Box)({
  display: 'flex',
  justifyContent: 'space-around',
  alignItems: 'center'
})

export default FlexArrow
