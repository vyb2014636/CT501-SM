import { styled } from '@mui/system'
import Button from '@mui/material/Button'

const FlexEndButton = styled(Button)({
  display: 'flex',
  justifyContent: 'flex-end',
  alignItems: 'center'
})

export default FlexEndButton
