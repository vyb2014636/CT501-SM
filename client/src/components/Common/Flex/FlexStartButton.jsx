import { styled } from '@mui/system'
import Button from '@mui/material/Button'

const FlexStartButton = styled(Button)({
  display: 'flex',
  justifyContent: 'flex-start',
  alignItems: 'center'
})

export default FlexStartButton
